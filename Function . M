% Define the function handle
f = @(x) sqrt(1 + x.^4) - x;
% Generate x values using linspace
x = linspace(1, 10^2, 10^6);
% Evaluate the function at the generated points
y = f(x);
% Display the function handle
disp(f);
